kind = "web"
previewPath = "/"
title = "SpecForge"
version = "1.0.0"
id = "artifacts/specforge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26218

[services.development]
run = "pnpm --filter @workspace/specforge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/specforge", "run", "build" ]
publicDir = "artifacts/specforge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26218"
BASE_PATH = "/"
